#include <functional>
#include "collections.hpp"

using namespace std;

namespace H {
	template <typename T>
	class Rect2D {
	private:
		T x, y, w, h;
		//List<function<void(this)>> listeners = new LinkedList();
	};
}